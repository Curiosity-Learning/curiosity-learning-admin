node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
._*
Thumbs.db

# IDE
.idea/

# Env
.env
.env.*
!.env.example

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Synced Convex API is COMMITTED (Vercel builds have no sibling main-repo checkout to sync
# from). Refresh with `npm run sync-api` after backend changes in the main repo and commit
# the result; never hand-edit.
